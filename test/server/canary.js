/* eslint-disable */
const expect = require('chai').expect
require('babel-polyfill')

describe('canary test', () => {
  it('should pass if env setup properly', () => {
    expect(true).to.be.true
  })
})
